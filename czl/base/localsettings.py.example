# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = '0123456789abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMN'

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = True

# Database
# https://docs.djangoproject.com/en/1.10/ref/settings/#databases
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'czl',
        #'USER': 'dbuser',
        #'PASSWORD': 'dbpass',
        #'HOST': '127.0.0.1',
        #'PORT': '5432',
    }
}

# you may want to use these settings for development to skip authentication.
# in production please observe the defaults in `settings.py`.
REST_FRAMEWORK = {
    'PAGE_SIZE': 20,
}

#STATIC_ROOT = '/webroot/static'
